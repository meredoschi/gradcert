<ul class="left">

	<% if can? :read, @user %>        
	
		<ul class="left">
	
			<% if is_active_link?(users_path, :inclusive) %>
	
				<li class="active">
				
			<% else %>
				
				<li>
	
			<% end %>
		 			 			   											
			  <%= link_to tm('user').pluralize.to_s.upcase, users_path  %></li>	
			    
	  </ul>    
	
	<% end %>
	 
</ul>
