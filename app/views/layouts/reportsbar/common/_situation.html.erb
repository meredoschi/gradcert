<% if ( is_active_link?(assessments_path, :inclusive) || is_active_link?(programsituations_path, :inclusive)  ) %>

	<li class="has-dropdown active">
	
<% else %>
	
	<li class="has-dropdown">

<% end %>

<a href="#"><%= t('navbar.external').mb_chars.upcase %></a>

	<ul class="dropdown">

		<%= render 'layouts/navbar/submenu/assessment.html.erb' %>
		<%= render 'layouts/navbar/submenu/program_situation.html.erb' %>
	
	  </ul>
	 </ul>
	</ul>
 </ul>