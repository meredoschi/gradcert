<% if ( is_active_link?(healthcareinfos_path, :inclusive) ||  is_active_link?(institutiontypes_path, :inclusive) || is_active_link?(colleges_path, :inclusive) || is_active_link?(researchcenters_path, :inclusive) || is_active_link?(characteristics_path, :inclusive) || is_active_link?(characteristics_path, :inclusive) ) %>

	<li class="has-dropdown active">
	
<% else %>
	
	<li class="has-dropdown">

<% end %>

<a href="#"><%= t('entity').pluralize.to_s.upcase %></a>

	<ul class="dropdown">

		<%= render 'layouts/navbar/submenu/institutions.html.erb' %>
		<%= render 'layouts/navbar/submenu/characteristics.html.erb' %>
	
		<%= render 'layouts/navbar/submenu/researchcenters.html.erb' %>
		<%= render 'layouts/navbar/submenu/healthcareinfos.html.erb' %>
		<%= render 'layouts/navbar/submenu/colleges.html.erb' %>
		<%= render 'layouts/navbar/submenu/institutiontypes.html.erb' %>

	
	  </ul>
	 </ul>
	</ul>
 </ul>