<div class="row">

	<%# if (can? :create, Registration) && registerable_students_for(current_user).count>0 && current_user.institution.with_programs? %>

	<% if (can? :create, Registration) %>

		<div class="large-8 columns">


		    	<h2> <%= t('activerecord.models.registration').pluralize.to_s.upcase  %> (<%= @registrations.total_count  %> /  <%= @numregistrations %>)</h2>

		</div>




			<div class="large-4 columns">
			 	<%= link_to image_tag('plus.png', class: "header-create-new", alt: t('actions.new.m')+' '+tm('registration')), new_registration_path %>

	 	 </div>

	<% else %>

			<h2> <%= t('activerecord.models.registration').pluralize.to_s.upcase  %> (<%= @registrations.total_count  %> /  <%= @numregistrations %>) </h2>




			     <%#= warn_if_no_programs %>
				   <%#= warn_if_no_students %>

	<% end %>

</div>
