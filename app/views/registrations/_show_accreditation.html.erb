<div class="row">

	<table>
		<tr>

				<div class="large-6 columns">
		
				
							<th> <%= ta('registration.accreditation.status') %> </th>

				</div>

				<div class="large-6 columns">

					<th><em><%= display_registration_status(@registration.accreditation) %></em></th>
					
				</div>
				
		</tr>						
	
	
			<% if @registration.accreditation.is_original_or_was_renewed? %>
			
				<%= render 'show_accreditation_start_date.html.erb' %>
			
			<% end %>
		
			<% if @registration.accreditation.was_suspended? %>
			
				<%= render 'show_accreditation_suspension.html.erb' %>
				
			<% end %>
			
			<% if @registration.accreditation.renewal.present? && @registration.accreditation.was_renewed? %>
		
				<%= render 'show_accreditation_renewal.html.erb' %>
			
			<% end %>
		
		
			<% if @registration.accreditation.was_revoked? %>
			
				<%= render 'show_accreditation_revocation.html.erb' %>
			
				<% if @registration.accreditation.suspension.present?	%>
			
					<%= render 'show_accreditation_suspension.html.erb' %>
			
				<% end %>
		
				<% if @registration.accreditation.renewal.present?	%>
			
					<%= render 'show_accreditation_renewal.html.erb' %>
			
				<% end %>
			
				<% if @registration.accreditation.start.present?	%>
			
					<%= render 'show_accreditation_start_date.html.erb' %>
			
				<% end %>
					
			<% end %>
			
</table>

<% if @registration.accreditation.comment.present? %> 

	<h5> <%= ta('accreditation.comment').capitalize %>: <%= @registration.accreditation.comment %>  </h5>

<% end %>

</div>
