<div class="row">

	<div class="large-8 columns">
		
		<h2><%= t('activerecord.models.role').capitalize.pluralize %> (<%= @roles.total_count  %> /  <%= @numroles %>)  	</h2>
	
  </div>

	<% if can? :create, Role %>

		<div class="large-4 columns">
			 	
	  		<%= link_to image_tag('plus.png', class: "header-create-new", alt: t('actions.new.m')+' '+tm('role')), new_role_path %>	 
	
	  </div>

	<% end %>
		  
</div>