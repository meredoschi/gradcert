<div class="row">

	<div class="large-8 columns">


	    	<h2> <%= t('activerecord.models.course').pluralize.mb_chars.upcase  %> (<%= @courses.total_count  %> /  <%= @numcourses %>)</h2>
 				
	</div>

	  <% if can? :create, Course %>

			<div class="large-4 columns">
			 	<%= link_to image_tag('plus.png', class: "header-create-new", alt: t('actions.new.m')+' '+tm('course')), new_course_path %>	 

	  </div>
		  
	<% end %>
	
</div>
