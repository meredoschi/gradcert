<h2> <%= t('noun.edit')+' '+tm('characteristic')%> </h2>

<% if local_admin?(current_user) %>

	<div class="row">

		<h2 class="subheader"><%= @characteristic.institution.name %> </h2>

	</div>

<% end %>

<%= render 'form' %>