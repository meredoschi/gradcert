<%= render 'index_header.html.erb' %>

<% if is_admin_or_manager(current_user) %>

		<%= render 'index_summary.html.erb' %>
<% end %>

<% if @placesavailables.total_count>0 %>

	<div class="row">

		<%= render 'index_table.html.erb' %>

	</div>

	<% if is_admin_or_manager(current_user) %>

		<%= render 'index_search.html.erb' %>

	<% end %>

<% end %>
