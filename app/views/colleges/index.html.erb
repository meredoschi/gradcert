<%= render 'index_header.html.erb' %>

<% if @colleges.total_count>0 %>

	<%= render 'index_table.html.erb' %>

	<% if is_admin_or_manager(current_user) && @colleges.total_count>1 %>

		<%= render 'index_search.html.erb' %>

	<% end %>
	
<% end %>