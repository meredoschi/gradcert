<h2> <%= t('noun.edit')+' '+tm('college')%> </h2>

<% if local_admin?(current_user) %>

	<div class="row">

		<h2 class="subheader"><%= @college.institution.name %> </h2>

	</div>

<% end %>

<%= render 'form' %>