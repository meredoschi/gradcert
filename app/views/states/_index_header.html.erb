<div class="row">

	<div class="large-8 columns">
		
		<h2><%= tm('state').capitalize.pluralize %> (<%= @states.total_count  %> /  <%= @numstates %>)  	</h2>
	
  </div>

	<% if can? :create, State %>

		<div class="large-4 columns">
			 	
	    	<%= link_to image_tag('plus.png', class: "header-create-new", alt: t('actions.new.f')+' '+tm('state')), new_state_path %>	 

	  </div>

	<% end %>
		  
</div>