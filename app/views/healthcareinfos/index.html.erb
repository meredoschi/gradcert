<%= render 'index_header.html.erb' %>

<% if @healthcareinfos.total_count>0 %>

	<%= render 'index_table.html.erb' %>

	<% if admin_or_manager?(current_user) && @healthcareinfos.total_count>1 %>

		<%= render 'index_search.html.erb' %>

	<% end %>
	
<% end %>