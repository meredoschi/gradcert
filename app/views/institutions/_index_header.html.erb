<div class="row">

	<div class="large-8 columns">

		<% if is_admin_or_manager(current_user) %> 

				
			<h2><%= t('entity').mb_chars.pluralize.upcase %></h2>
		
			<h2 class="subheader"><%= t('institution_summary').capitalize.pluralize(@institutions.total_count).pluralize.mb_chars.capitalize %> (<%= @institutions.total_count  %> /  <%= @numinstitutions %>)  	</h2>

		<% else %>

			<h2><%= t('my.f').mb_chars.upcase+' '+tm('institution').mb_chars.upcase %></h2>
		
			<h2 class="subheader"><%= t('summary').capitalize %></h2>
						
		<% end %>
		
	</div>

		<div class="large-4 columns">
	
			<% if can? :create, @institution %>
		 	
			 	<%= link_to image_tag('plus.png', class: "header-create-new", alt: t('actions.new.f')+' '+tm('institution')), new_institution_path %>	 

			<% end %>
	  
	  </div>
		  
</div>