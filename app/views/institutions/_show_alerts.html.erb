<% if !@institution.with_infrastructure? && not_regular_user?(current_user)  %>

	<div class="row alert-box warning">
	
		<strong><%=  t('no_infrastructure_reported') %> (<%=tm('researchcenter') %>, <%=tm('healthcareinfo') %>, <%=tm('college') %>)</strong>

	</div>

<% end %>

<% if !@institution.with_characteristic? && not_regular_user?(current_user)  %>

	<div class="row alert-box warning">
	
		<strong><%=  t('characteristics_not_on_file') %> </strong>

	</div>

<% end %>

<% if !@institution.accreditation.details_on_file? && not_regular_user?(current_user) %>

	<div class="row alert-box alert">
	
		<strong><%=  t('accreditation_not_on_file') %></strong>

	</div>
	
<% end %>