<div class="row">	    		
	<div class="large-3 columns", class: "left inline">
			<%= f.label tm('institution') %>
	</div>
			
	<div class="large-9 columns">								
		<%= f.select :institution_id_eq, options_from_collection_for_select(Institution.joins(:program).distinct, :id, :name), 		include_blank: true %>				
	</div>
		
  <%# to do: filter for active programs and show unique values %>
		
</div>