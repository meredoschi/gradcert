<div class="row">

		<h5>

			<div class="large-3 columns", class: "left inline">

		  	<%= tm('student').capitalize  %>

			</div>

		 	<div class="large-9 columns">

				<% if !is_manager(current_user) || action_name=='new' %>

		 			<%#= f.collection_select :registration_id, registrations_for(current_user), :id, registration_details(current_user),  {prompt: true} %>

					<%# to do: verify model and application helper definitions --> registration_details(current_user)%>
					<%# quick fix start %>

					<% if is_local_admin(current_user) %>

						<%= f.collection_select :registration_id, active_registrations_for(current_user), :id, :student_name_id_schoolyear_term,  {prompt: true} %>

					<% end %>

					<% if is_admin_or_manager(current_user) %>

						<%= f.collection_select :registration_id, active_registrations_for(current_user), :id, :student_name_schoolyear_institution_abbrvs,  {prompt: true} %>

					<% end %>

					<%# quick fix end %>

				<% else %>

					<%= @event.registration.details %>

				<% end %>
			</div>

		</h5>


</div>

</br>
