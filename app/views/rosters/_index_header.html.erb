<div class="row">

	<div class="large-8 columns">


	    	<h2> <%= tm('roster').pluralize.mb_chars.upcase  %> (<%= @rosters.total_count  %> /  <%= @numrosters %>)</h2>
 				
	</div>

	  <% if can? :create, Roster %>

			<div class="large-4 columns">
			 	<%= link_to image_tag('plus.png', class: "criar", alt: t('actions.new.m')+' '+tm('roster')), new_roster_path %>	 

	  </div>
		  
	<% end %>
	
</div>
