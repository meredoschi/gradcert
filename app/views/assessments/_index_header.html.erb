<div class="row">

	<div class="large-8 columns">

		<% if admin_or_manager?(current_user) %>

	    	<h2> <%= tm('assessment').pluralize.to_s.upcase  %> 
 				
 		<% else %>
 		
	    	<h2> <%= t('my.mp').capitalize+" "+tm('assessment').pluralize %>
	  		
  	<% end %>

	(<%= @assessments.total_count  %> /  <%= @numassessments %>)</h2> 		

	</div>

	  <% if can? :create, Assessment %>

			<div class="large-4 columns">
			 	<%= link_to image_tag('plus.png', class: "header-create-new", alt: t('actions.new.f')+' '+tm('assessment')), new_assessment_path %>	 

	  </div>
		  
	<% end %>
	
</div>