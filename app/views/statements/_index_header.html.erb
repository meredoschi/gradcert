<div class="row">

	<div class="large-8 columns">


	    	<h2> <%= tm('statement').pluralize.to_s.upcase  %> (<%= @statements.total_count  %> /  <%= @numstatements %>)</h2>
 				
	</div>

	  <% if can? :create, Statement %>

			<div class="large-4 columns">
			 	<%= link_to image_tag('plus.png', class: "criar", alt: t('actions.new.m')+' '+tm('statement')), new_statement_path %>	 

	  </div>
		  
	<% end %>
	
</div>
